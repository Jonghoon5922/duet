# Duet — 스펙 (초안 v2)

> 나와 Claude가 함께 하는 일을 타스크 단위로 묶고, 세션이 다 끝나면 타스크가 닫히는 **개인용 로컬 PMS**.
> MCP로 세션이 스스로 상태를 보고하고, 대시보드에서 사람이 한눈에 보고 고친다.
> 패키지명 `duet-mcp`, CLI 명령 `duet`.

> **이 문서는 2026-09-09의 설계 초안이다.** 만들면서 바뀐 것이 많다 — 저장 배치(§5), 상태 이름(§3),
> 도구 목록(§6), 보드(§7), CLI(§8). **지금 모습은 README.md와 CLAUDE.md가 맞고**, 이 문서는 왜 이렇게
> 만들었는지(§2·§4·§9)를 보려고 남겨 둔다. 어긋난 절에는 메모를 달았다.

## 0. 시작 전 확인 (Sprintra)

같은 자리에 [Sprintra](https://sprintra.io)가 있다 — MCP 네이티브, 로컬 우선 SQLite, 에이전트 프레즌스, 세션 리플레이, 스프린트·칸반. 개인 무료.
**1단계 코드를 쓰기 전에 하루 써보고** 아래 세 가지를 확인한다. 셋 다 되면 Duet은 만들지 않는다.

1. 세션 여러 개를 타스크 하나에 묶고, 전부 끝나면 타스크가 자동으로 닫히는가
2. 창을 그냥 닫은 세션을 "중지"로 잡아주는가
3. 대시보드에서 상태를 직접 고칠 때 AI 쪽 갱신과 충돌하지 않는가

안 되는 것이 Duet의 정확한 범위다. 확인 결과를 이 절 아래에 기록한다.

### 확인 결과 (2026-09-09)

**Sprintra 확인은 건너뛰고 Duet을 만든다.** 사용자 결정.
남의 도구가 세 가지를 하느냐와 무관하게, 내 기준(개인 전용·파일 기반·세션이 1급 객체)으로 만든다.
0단계는 여기서 닫는다.

## 1. 한 줄 정의

사용자가 타스크를 만들면, 여러 Claude 세션이 각자 MCP로 그 타스크에 **참여(join)** 하고 진행·완료를 보고한다.
타스크에 묶인 세션이 전부 완료되면 타스크도 자동 완료. 대시보드(로컬 웹)에서 타스크 전체를 상태별로 보고 직접 편집한다.
**이 도구는 LLM을 호출하지 않는다. 호스팅 서버도, DB 서버도 없다.** 데이터는 로컬 파일이다.
**개인 전용이다.** 팀 동기화 층은 만들지 않는다 (Sprintra와 갈라지는 지점).

## 2. 왜 이 도구인가

개인 할 일 앱은 많다. 이 도구가 있어야 할 이유는 하나다:

**세션이 1급 객체다.** 타스크는 "세션들의 묶음"이고, 세션은 사람이 아니라 Claude가 열고 닫는다.
- 세션이 타스크에 붙는 순간 타스크가 `진행중`이 된다
- 세션이 완료를 보고하면 세션이 닫히고, 마지막 세션이 닫히면 타스크가 닫힌다
- 세션이 보고 없이 사라지면(창 닫음, 크래시) 도구가 감지해 `중지`로 표시한다

"내가 Claude를 몇 개 띄워서 뭘 시켰고 지금 어디까지 왔나"가 손대지 않아도 보인다.

## 3. 개념 모델

> 메모: 타스크 상태는 `대기·진행중·확인 필요·완료·보류·취소`, 세션은 `진행중·완료·중지`. 사람은 완료·보류·취소만 고른다. 화면에서는 영문으로 보인다.


```
타스크 (task)
 ├── 세션 A  ─ 진행중  "DBIO 계층 전환"      마지막 보고 3분 전
 ├── 세션 B  ─ 완료    "Bean 계층 전환"      14:20 완료
 └── 세션 C  ─ 중지    "설계서 docx 빌드"    프로세스 종료 감지, 미완료
```

### 상태

| 대상 | 상태 | 전이 |
|---|---|---|
| 타스크 | `대기` → `진행중` → `확인 필요` / `완료`, 그리고 사람만 정하는 `보류` / `취소` | 첫 세션 join 시 `진행중`. 끊긴 세션이 남으면 `확인 필요`. 모든 세션 완료 시 `완료`. (2026-09-10: 타스크의 `중지`를 `보류`로, `확인 필요`를 정식 상태로) |
| 세션 | `진행중` → `완료` / `중지` | Claude가 `complete_session` 호출 시 `완료`. 프로세스 종료·하트비트 끊김 시 `중지`. 사람이 `중지`를 `완료`로 바꿀 수 있음 |

**자동 완료 규칙**: 세션이 1개 이상이고 전부 `완료`이면 타스크 `완료`. 하나라도 `중지`면 `확인 필요` (사람이 판단).
**한 번에 하나** (2026-09-10 결정): 타스크 하나에 살아 있는 세션은 하나뿐이다. 두 번째 세션은 앞 세션이 `완료`/`중지`가 된 뒤에야 붙는다 — 그래서 "세션들의 묶음"은 동시가 아니라 순서다. 세션 하나는 타스크 하나만 한다. 한 창이 다음 타스크를 잡으면 새 세션이 된다(같은 창이라는 건 대화 id가 묶는다). 프로젝트 안에서는 창 여럿이 각자 다른 타스크를 동시에 한다.
**수동 우선**: 사람이 대시보드에서 바꾼 상태는 자동 규칙보다 우선. `수동 잠금`이 걸리면 세션 이벤트가 덮어쓰지 않는다.

## 4. 세션 식별 (핵심 설계)

MCP(stdio)는 **클라이언트 세션마다 서버 프로세스가 하나씩 뜬다.** 그래서 **서버 프로세스 = 세션**.

- 서버가 뜰 때 세션 ID를 만들고 자기 세션 파일을 생성 (`미참여`)
- Claude가 `join_task(task_id, title)`를 부르면 세션 파일이 그 타스크 폴더로 이동
- 서버는 30초마다 자기 세션 파일에 하트비트를 씀. 프로세스 종료 훅(stdin EOF·시그널)에서 `중지` 기록. 훅조차 못 돌면 대시보드가 하트비트 90초 초과를 `중지`로 판정
- Claude Code·Claude Desktop·Cursor·bxca 어디서 띄워도 동일. 클라이언트 종류는 부모 프로세스명·환경변수로 추정해 표시만

서버 instructions 주입:

```
열린 타스크: [3] NEFSS pc101pm 전환 (세션 2개 진행중), [5] 서재 임베딩 옵션 (세션 0개)
이 세션은 아직 어떤 타스크에도 참여하지 않았다. 사용자가 작업을 지시하면 어느 타스크인지 확인하고
join_task를 호출하라. 새 일이면 create_task 후 join_task. 작업 중간에 report_progress로 한 줄씩 남기고,
끝나면 complete_session을 호출하라. 사용자가 타스크를 언급하지 않으면 묻지 말고 작업을 먼저 하되,
첫 보고 시점에 한 번만 확인한다.
```

## 5. 저장 — DB 없음, 파일만

> **구현 메모 (2026-09-10):** 실제 배치는 아래 초안과 다르다 — `tasks/`·`sessions/` 층 없이
> `~/.duet/<프로젝트>/T001-슬러그/{task.md, s-*.json}` 이다. 프로젝트가 폴더 하나, 번호는 프로젝트
> 안에서 1부터, 보관은 `_보관/<프로젝트>/`로 폴더째, 프로젝트를 모르면 `_미분류/`. 부르는 이름은
> `nefss/T001`. 살아 있는 문서는 README.md의 「저장」·「프로젝트」 절이다.

```
~/.duet/
├── tasks/
│   ├── T003-pc101pm-전환/
│   │   ├── task.md               ← 타스크 정의. 프론트매터: id, title, status_override, locked, tags, created
│   │   │                            본문: 설명 + "## 이력" 절(상태 전이 로그, 누가·언제)
│   │   └── sessions/
│   │       ├── s-a1b2.json       ← 세션 A의 프로세스만 씀
│   │       ├── s-c3d4.json       ← 세션 B의 프로세스만 씀
│   │       └── s-e5f6.json
│   └── T005-서재-임베딩/
│       └── task.md
└── unassigned/
    └── s-9f8e.json               ← 아직 join 안 한 세션
```

**충돌이 없는 이유**: 세션 파일은 그 세션 프로세스만 쓴다. `task.md`는 사람·대시보드·Claude(`update_task`)만 쓴다.
**타스크 상태는 저장하지 않고 계산한다**: 세션 파일들을 읽어 자동 규칙 적용. `status_override`가 있으면 그것이 우선.

세션 파일:
```json
{
  "id": "s-a1b2", "title": "DBIO 계층 전환", "client": "claude-code",
  "status": "진행중", "started": "2026-09-09T14:02:11", "heartbeat": "2026-09-09T14:31:40",
  "completed": null, "summary": null,
  "progress": [
    {"t": "14:05", "msg": "Qp0036 설계서 작성", "pct": 30},
    {"t": "14:20", "msg": ".dbio/.omm 생성, 컴파일 통과", "pct": 80}
  ]
}
```

- 대시보드·CLI는 폴더를 읽고 파일 감시(watchdog)로 갱신
- 타스크 폴더를 사람이 열어 `task.md`를 직접 고치는 것도 정식 편집 방법
- 규모 한계: 타스크 수천 개면 스캔이 느려짐 — 개인용에서는 도달하지 않음

## 6. MCP 도구

> 메모: 실제 도구는 `list_tasks·create_task·join_task·get_task·update_task·report_progress·complete_session·pause_session` 여덟 개. `tags`·이력 절은 없다. 타스크는 `duet/T001`로 부른다.


| 도구 | 입력 | 동작 |
|---|---|---|
| `list_tasks` | `status?` | 타스크 목록 (세션 수·상태·마지막 활동) |
| `create_task` | `title`, `description?`, `tags?` | 타스크 폴더 + `task.md` 생성 (`대기`) |
| `join_task` | `task_id`, `session_title` | 이 세션 파일을 타스크로 이동. 타스크 `진행중` |
| `report_progress` | `message`, `percent?` | 이 세션 파일의 `progress`에 한 줄 추가 |
| `complete_session` | `summary` | 이 세션 `완료`. 자동 완료 규칙은 읽는 쪽이 계산 |
| `pause_session` | `reason` | 이 세션 `중지` (사용자가 "여기까지" 할 때) |
| `get_task` | `task_id` | 타스크 상세: 세션 목록·각 세션 로그. **다른 세션이 뭘 했는지 읽어 인수인계** |
| `update_task` | `task_id`, `status?`, `title?`, `description?` | `task.md` 편집 + 이력 절에 기록 |

## 7. 대시보드 (로컬 웹, 127.0.0.1)

> 메모: 프로젝트 상자 + 타스크 줄. 상태 칩 필터, 검색, +New Project, +Task, Archive, ×, Feedback. 2초 폴링(SSE는 아직). 글은 영문.


`duet ui` 또는 `duet app`(자체 창, 서재 방식).

**타스크 보드**
- 상태 필터: `전체 / 대기 / 진행중 / 중지 / 완료 / 취소` + 태그 + 검색
- 카드: 제목, 상태, 세션 수(진행중/완료/중지), 마지막 활동, 진행률(세션 완료 비율 또는 pct 평균)
- 카드에서 상태 변경(드롭다운), 제목·설명 인라인 편집, 수동 잠금 토글
- 정렬: 마지막 활동순 / 생성순 / 상태순

**타스크 상세**
- 세션 목록: 제목, 클라이언트, 상태, 시작·마지막 하트비트·완료 시각
- 세션별 진행 로그 타임라인
- 세션 상태 수동 변경, "이 타스크 닫기"(세션 전부 종료 처리)

**실시간**: 파일 감시 → SSE로 새로고침 없이 갱신

## 8. CLI

> 메모: `serve·ui·add·list·show·version·mcp-register·mcp-unregister·mcp-list`. `set`·`sessions`는 없다 — 보드가 대신한다.


| 명령 | 동작 |
|---|---|
| `duet serve` | MCP(stdio) 서버. 세션 파일 생성·하트비트 |
| `duet ui` / `duet app` | 대시보드 |
| `duet list [--status]` | 타스크 목록 |
| `duet add <title>` | 타스크 생성 |
| `duet set <task_id> <status>` | 상태 변경 (수동 잠금) |
| `duet sessions [task_id]` | 세션 현황 (하트비트 생존 표시) |

## 9. 설계 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 세션 식별 | MCP 서버 프로세스 = 세션 | 클라이언트 무관, 훅 설정 불필요 |
| 세션 사라짐 감지 | 종료 훅 + 하트비트 90초 | 창 닫기·크래시 모두 |
| 저장 | 로컬 파일. 세션 파일은 자기 프로세스만 씀 | DB 없이 충돌 없음. 사람이 읽고 고칠 수 있음 |
| 타스크 상태 | 저장 안 함, 세션에서 계산. 수동 변경만 저장 | 자동·수동 충돌을 구조로 제거 |
| 자동 완료 | 전부 `완료`일 때만. `중지` 있으면 사람 판단 | 실패한 세션을 뭉개지 않게 |
| 팀 공유 | 안 만듦 | 개인 전용. Sprintra와 갈라지는 지점 |
| LLM 호출 | 없음 | A 구조 |

## 10. 구현 단계

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 0 | Sprintra 하루 사용, §0 세 항목 확인·기록 | 계속 만들지 결정 |
| 1 | 파일 구조, `create_task`/`list_tasks`/`join_task`/`report_progress`/`complete_session`, 세션 파일·하트비트·종료 감지 | Claude Code 세션 2개가 같은 타스크 join → 둘 다 완료 → 타스크 자동 완료 실측 |
| 2 | `get_task`/`update_task`/`pause_session`, instructions 주입, 중지 감지 | 창을 닫은 세션이 90초 내 `중지` |
| 3 | 대시보드 보드·필터·인라인 편집·수동 잠금·SSE | 편집 즉시 반영, 세션 보고가 새로고침 없이 뜸 |
| 4 | 타스크 상세·세션 타임라인·`app` 창 | |
| 5 | PyPI `duet-mcp`, `.mcpb`, 인스톨러 (서재 방식 재사용) | |

## 11. 검증 시나리오

1. 대시보드에서 타스크 "pc101pm 전환" 생성
2. Claude Code 세션 A "DBIO 전환해줘" → join, report 3~4건, complete
3. 세션 B "Bean 전환해줘" → join. `get_task`로 A의 로그를 읽고 이어서 → complete
4. 대시보드에서 타스크 `완료`, 필터 `완료`에 잡힘
5. 세션 C를 join만 하고 창을 닫음 → 90초 내 `중지`, 타스크는 `진행중` + 경고

## 12. 추후 (지금은 하지 않음)

- 서재 연동: `~/.duet/tasks`를 책장으로 등록하면 "지난달 뭐 했지"가 검색됨. 파일 기반이라 export 없이 바로 가능
- 일지 연동: 주간 일지에 "어떤 타스크에 세션 몇 개를 썼는지"
- 카카오톡 알림: 세션 완료·중지 시 나에게 보내기
- 팀 공유: 공유 폴더·git으로 세션 파일을 나누는 방식은 구조상 가능하나 범위 밖
